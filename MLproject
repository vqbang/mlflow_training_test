name: mlflow-model

docker_env:
  image: bangvo/base-image

entry_points:
  main:
    parameters:
      alpha: float
      l1_ratio: {type: float, default: 0.1}
    command: "python3 train.py --alpha {alpha} --l1-ratio {l1_ratio}"
